export const bottomToTopProps =  {
    immediate: false,
    from: { opacity: 0, transform: "translateY(20px)" },
    to: { opacity: 1, transform: "translateY(0px)" },
    delay: 200,
    config: { duration: 800 }
};

export const leftToRightProps = {
    immediate: false,
    from: { opacity: 0, transform: "translateX(-20px)" },
    to: { opacity: 1, transform: "translateX(0px)" },
    delay: 200,
    config: { duration: 800 },
};

export const topToBottomProps = {
    immediate: false,
    from: { opacity: 0, transform: "translateY(-20px)" },
    to: { opacity: 1, transform: "translateY(0px)" },
    delay: 200,
    config: { duration: 800 },
};

export const rightToLeftProps = {
    immediate: false,
    from: { opacity: 0, transform: "translateX(20px)" },
    to: { opacity: 1, transform: "translateX(0px)" },
    delay: 200,
    config: { duration: 800 },
};

export const topToBottom2Props = {
    immediate: false,
    from: { opacity: 0, transform: "translateY(-40px)" },
    to: { opacity: 1, transform: "translateY(0px)" },
    delay: 200,
    config: { duration: 800 },
};

export const Props = {
    immediate: false,
    from: { opacity: 0 },
    to: { opacity: 1 },
    delay: 200,
    config: { duration: 800 },
};

